package main

import "fmt";

func main() {
  myFunction([2]int{1, 2})

  var odds [4]int = [4]int{2: 5, 3: 7};

  const (
  	b int = 0;
  	c int = 1;
  	d string = "Go(lang)vno";
  );

  var a int = 1;
  
  for ; a < 0; a++  {
    if a > 5 { a--; }
  }
};

func myFunction(a [4]int) {
  fmt.Println(a);
  a[1] = 0;
};